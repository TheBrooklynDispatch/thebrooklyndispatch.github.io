---
layout: base.njk
title: Film
description: Reviews, criticism and dispatches on international and independent cinema.
permalink: /film/
---

{% set films = collections.articles | sortByPublishedDate %}

<div class="wrap">

  <section class="section-intro">

    <div class="eyebrow">
      Section
    </div>

    <h1>
      Film
    </h1>

    <p>
      Reviews, criticism and dispatches on international and independent cinema.
    </p>

  </section>

  <div class="film-grid">

    {% for article in films %}

      {% if article.data.category == "Film" %}

        <article class="film-card">

          {% if article.data.image %}
            <a href="{{ article.url }}" class="film-card__image-link">
              <img
                class="film-card__image"
                src="{{ article.data.image }}"
                alt="{{ article.data.title }}">
            </a>
          {% endif %}

          <div class="meta-label">
            Film · {{ article.date | readableDate }}
          </div>

          <h2>
            <a href="{{ article.url }}">
              {{ article.data.title }}
            </a>
          </h2>

          {% if article.data.dek %}
            <p>
              {{ article.data.dek }}
            </p>
          {% endif %}

          {% if article.data.stars %}
            <div class="article-rating article-rating--small">
              {% if article.data.stars == 5 %}★★★★★{% endif %}
              {% if article.data.stars == 4.5 %}★★★★½{% endif %}
              {% if article.data.stars == 4 %}★★★★{% endif %}
              {% if article.data.stars == 3.5 %}★★★½{% endif %}
              {% if article.data.stars == 3 %}★★★{% endif %}
              {% if article.data.stars == 2.5 %}★★½{% endif %}
              {% if article.data.stars == 2 %}★★{% endif %}
              {% if article.data.stars == 1.5 %}★½{% endif %}
              {% if article.data.stars == 1 %}★{% endif %}
              <span class="article-rating__out-of">{{ article.data.stars }}/5</span>
            </div>
          {% endif %}

          <a class="text-link" href="{{ article.url }}">
            Read →
          </a>

        </article>

      {% endif %}

    {% endfor %}

  </div>

</div>
```
